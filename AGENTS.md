# x-to-md Agent Instructions

在本仓库工作前，必须完整读取并遵循 `.codexrules`。

它是本项目的唯一仓库级规则入口。项目上下文位于 `memory-bank/`；只有任务需要时按 `memory-bank/README.md` 的路由读取。

## 产品形态与视觉铁律

- 插件的主工作界面必须使用 Chrome Side Panel；不得将订阅源、候选集或素材库设计为独立后台页面或 Chrome Action Popup。
- `x-to-md` 的产品视觉、信息密度、三栏布局、字体、间距、分隔线、控件状态和交互语义必须像素级复刻当前 X Web 界面，使用户在 X 与插件 Side Panel 间无缝切换。
- 任何新增界面、原型或实现前，必须以当前 X Web 的真实页面为视觉基准；不得以通用 SaaS、卡片式仪表盘或自创设计语言替代 X 的产品理念与原则。

## 大型 X 页面浏览器验收

- 对 X 等持续更新的大型 SPA，不得把整页 `domSnapshot()` 作为默认读取方式；先明确要验证的单一事实，再使用 URL、标题、视口截图、唯一 locator 状态或有上限的 `evaluate()` 投影取证。
- 页面导航与状态读取必须拆成独立调用。X 页面不等待 `networkidle`；导航后先验证 URL/标题，再等待目标元素或读取目标状态。
- 数据裁剪必须发生在页面内：查询后先 `slice`/限制条数，再返回只含必要字段的 JSON。禁止在完整 `domSnapshot()`、`body.innerText` 或大数组返回后才截断字符串。
- 只有构造 locator 确实缺少 DOM 依据时，才允许读取一次新快照；复用仍然有效的快照。一次快照超时后不得原样重试，必须改用局部 locator、受限 `evaluate()`、可见 DOM 或视口截图。
- 不使用全页截图验证 X 时间线。执行内核因超时重置后，重新获取浏览器与标签页，并切换到更小的取证方式；不得继续重复造成重置的调用。
