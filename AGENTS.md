# x-to-md Agent Instructions

本文件是本仓库唯一自动加载的项目规则入口。系统、开发者和当前用户指令优先于本文件。

项目上下文位于 `memory-bank/`，只有任务需要时按 `memory-bank/README.md` 的路由读取。

## 授权与范围

- 审查、分析、复盘和计划请求保持只读；只有用户明确要求修改、实现或执行时才编辑文件。
- 修改前检查 `git status --short`，保护用户已有改动；未经明确要求，不创建或切换分支，不提交、不推送、不发布。
- 本仓库独立于 `x-to-xhs`；不得重新建立源码、测试、构建或运行时依赖。
- `manifest.json` 的权限、主机范围和版本号是发布契约；新增权限或扩大 `host_permissions` 必须说明原因、补充测试并获得用户明确授权。

## 按需读取路由

以下文件不会自动加载，只在触发条件成立时读取：

| 任务 | 修改或操作前必须读取 |
|---|---|
| 修改代码、测试、规则或执行浏览器/视觉验收 | `docs/development/validation.md` |
| 修改 X DOM、Card、Grok 左侧入口或独立菜单 | `docs/development/x-dom-integration.md` |
| 修改模块职责、消息协议、存储键、持久化或一次性预览 | `docs/development/repository-contracts.md` |
| 读取、修改或归档 `memory-bank/**`，或查看下一任务 | `memory-bank/README.md` |

默认不读取 `memory-bank/archive/**`；只有用户明确指定归档文件，或任务本身是已授权的归档维护时才访问。

当用户要求“查看下一任务”时，先读取 `memory-bank/README.md`，再只读取 `memory-bank/progress.md` 的“当前待办”，复述第一个未完成项目及验收范围；“查看”保持只读，只有用户明确要求执行后才修改。

## 阶段复盘

- 用户启动复盘、阶段总结、事后分析或知识沉淀时，使用全局 `spec-driven-solo-skill` Skill。
- 项目硬约束与读取路由沉淀到本文件；X DOM、模块、消息和存储等详细事实进入对应 `docs/development/` 文档；当前状态进入 `memory-bank/`；可机械验证的边界进入测试。
- 不把完整过程日志、临时错误或一次性实现细节写入本文件。迁移既有规则时不得静默删除或弱化，删除前必须获得用户明确确认。

## 产品形态与视觉铁律

- 插件的主工作界面必须使用 Chrome Side Panel；不得将订阅源、候选集或素材库设计为独立后台页面或 Chrome Action Popup。
- `x-to-md` 的产品视觉、信息密度、三栏布局、字体、间距、分隔线、控件状态和交互语义必须像素级复刻当前 X Web 界面，使用户在 X 与插件 Side Panel 间无缝切换。
- 任何新增界面、原型或实现前，必须以当前 X Web 的真实页面为视觉基准；不得以通用 SaaS、卡片式仪表盘或自创设计语言替代 X 的产品理念与原则。

## X 独立动作入口契约

- Home、作者 Posts/Articles 列表和 `/status`、`/article` 原文页的 x-to-md 入口位于 `Grok actions` 左侧。
- “独立入口”同时要求独立 DOM、交互状态和菜单生命周期；可以复用 Grok 的视觉结构，不得复用其 React 状态、原生 caret 点击或 Dropdown 生命周期。
- 点击 x-to-md 入口不得调用原生 More，不得通过打开后隐藏原生菜单项实现插件菜单。
- 菜单动作只读取被点击入口所属的 Post 或 Article；同一 Card 最多注入一个入口。
- 核心入口不得依赖 hover、mousemove 或用户滚动生成；SPA 注入必须支持等待 `main`、扫描现有 Card、局部观察新增 Card、幂等注入和实例释放。
- 菜单必须支持再次点击切换、点击外部关闭、`Escape` 关闭以及滚动或缩放后的安全关闭。

## 产品与数据安全

- 扩展只处理用户置于当前任务范围内的 X/Twitter 页面，不调用 X API、oEmbed、公共代理或自建上传服务。
- 不收集、记录或上传用户正文、Cookie、令牌、账号信息和剪贴板内容。
- 不添加远程脚本、远程代码执行、跟踪分析或不必要的网络权限。
- Popup、Content Script、Background、Side Panel 与 Preview 的消息或存储键属于跨模块契约；变更时必须同步测试和项目技术上下文。

## 实现与验证纪律

- 优先精确检索和局部读取；不进行无目标全仓扫描，不输出完整大文件、完整 DOM、完整差异或完整失败日志。
- 禁止无过滤执行 `rg --files`、`find` 或针对 Markdown 的全局扫描；第一次多文件检索默认至多携带 2 行上下文，单次源码或差异读取默认不超过 160 行，普通工具输出默认不超过 4000 tokens。正确性确实需要时才扩大范围。
- 修改前选择满足需求的最小方案；连续两轮围绕同一症状进行局部修补仍未解决时，停止调整 CSS、选择器或时序，重新检查架构假设。
- 测试优先验证用户行为和数据契约，不锁定非必要函数名、临时选择器或第三方页面实现细节。
- JavaScript、交互、采集或 Manifest 修改运行 `npm test` 和 `git diff --check`；纯文档或规则修改运行 `git diff --check`。
- 开发中先运行定向验证；相关代码和环境未变化时不重复完整验证，最终完整验证最多保留一次成功结果。
- 除非用户明确授权，不执行 Chrome 实机验收；静态测试不能表述为真实 X 页面兼容性已通过。

## 大型 X 页面浏览器验收

- 对 X 等持续更新的大型 SPA，不得把整页 `domSnapshot()` 作为默认读取方式；先明确要验证的单一事实，再使用 URL、标题、视口截图、唯一 locator 状态或有上限的 `evaluate()` 投影取证。
- 页面导航与状态读取必须拆成独立调用。X 页面不等待 `networkidle`；导航后先验证 URL/标题，再等待目标元素或读取目标状态。
- 数据裁剪必须发生在页面内：查询后先 `slice`/限制条数，再返回只含必要字段的 JSON。禁止在完整 `domSnapshot()`、`body.innerText` 或大数组返回后才截断字符串。
- 只有构造 locator 确实缺少 DOM 依据时，才允许读取一次新快照；复用仍然有效的快照。一次快照超时后不得原样重试，必须改用局部 locator、受限 `evaluate()`、可见 DOM 或视口截图。
- 不使用全页截图验证 X 时间线。执行内核因超时重置后，重新获取浏览器与标签页，并切换到更小的取证方式；不得继续重复造成重置的调用。
