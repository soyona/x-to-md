# x-to-md：Codex 项目规则

本文件是本仓库的规则入口。系统、开发者和当前用户指令优先于本文件。

# Codex 项目硬性约束

## 记忆术语与授权范围

1. **Codex 全局记忆**：指 Codex 管理的仓库外跨项目记忆，默认位于
   `~/.codex/memories/**`。其读写遵循上层 Codex 全局记忆规则。

2. **项目 Memory Bank**：仅指当前仓库内的 `./memory-bank/**`。
   它属于项目文档，不属于 Codex 全局记忆。

3. 当用户已经授权实现、修改或执行当前项目任务时，仅与该任务直接相关、
   且本文件明确要求的最小 Memory Bank 同步，属于当前项目任务范围，
   不需要取得“更新 Codex 全局记忆”的额外授权。

4. 第 3 条不得被解释为对整个 Memory Bank 的概括授权：
   - 不得更新与当前任务无关的项目记忆；
   - 不得借同步之名整理、改写或扩展无关历史；
   - `memory-bank/archive/` 是行为隔离区，不是文件系统安全边界；
   - 默认不得扫描、搜索或读取归档内容，只有用户明确指定归档文件，
     或当前任务就是已授权的归档维护时，才允许按指定范围访问。

5. 审查、分析、复盘和计划等只读任务不得修改项目 Memory Bank。
   只读过程中发现同步或归档需求时，仅在最终回复中报告。

## 指令优先级与授权边界

1. 系统、开发者和用户当前指令高于本文件；本文件高于项目记忆和一般文档。
2. 审查、分析、复盘和计划请求保持只读；只有用户明确要求修改、实现或执行时才编辑文件。
3. 未经用户明确要求，不创建或切换分支，不提交、不推送、不发布。

## 默认工作预算与重复操作控制

第 1 项是启动读取边界，只适用其中明确列出的例外。第 2–6 项规定的是
默认工作预算，不是正确性边界；出现测试失败、工具报错、源码歧义、
用户修改、跨文件调用关系或当前任务明确要求方案比较时，可以突破默认
预算，但不得无理由重复读取、重复验证或扩大任务范围。

1. **启动读取边界**
   - 在仓库自主读取范围内，收到具体需求前只读本文件。
   - 用户要求“查看下一任务”时，按下文“下一任务发现规则”额外读取当前待办。
   - 更高优先级指令要求的上下文除外。

2. **精确读取预算**
   - 优先使用 `rg -n` 定位具体函数、变量或文件，再一次性读取完成任务所需的局部区间。
   - 同一源码文件在编辑前默认完成一次定位和一次必要上下文读取；只有出现
     测试或构建指向、工具失败、用户修改、明确歧义或新的跨文件证据时才继续读取。
   - 第一次检索默认只输出文件、行号和命中内容；需要上下文时再读取命中附近片段。
   - 多文件检索默认不同时使用超过 2 行上下文；单次源码或差异读取默认不超过
     160 行。任务正确性确实需要时可以扩大，但应先缩小目标范围。
   - 大文件先精确定位，再按小范围行号读取；输出截断后优先缩小范围，不重复
     提高上限运行同一命令。

3. **工具输出预算**
   - 普通检索单次输出默认不超过 4000 tokens；失败详情或正确性判断确实需要时
     可以提高。
   - 默认查看 `git diff --stat`、`git diff --check` 和指定文件的关键差异；
     只有当前审查确实需要完整变更上下文时才扩大差异范围。
   - 不重复输出完整测试日志或已经成功读取的内容。
   - 不为最终回复专门重新读取源码行号；文件链接默认不附行号。

4. **实现方案预算**
   - 修改前优先选择满足当前需求的最小方案。
   - 只有当前方案无法验证、存在实质性权衡或用户明确要求比较时，才评估替代方案。

5. **验证预算**
   - 开发过程中优先运行与改动直接相关的定向测试。
   - 代码和相关环境均未变化时，默认不重复运行同一完整验证；相关代码、
     测试环境或失败证据发生变化后可以重跑。
   - 修改成功后优先运行 `git diff --check`、`git diff --stat` 和对应的定向测试
     或构建，不重新输出源码。
   - 最终成功状态只保留一次完整验证结果；不得为了确认同一既有失败而重复执行。

6. **沟通预算**
   - 中间进度只报告假设、关键里程碑和阻塞，不逐项复述工具操作。
   - 编码和修改任务的最终回复优先包含：改动、验证、风险、未执行项目。
   - 审查、诊断、解释和计划任务按当前用户请求组织回复。

## 🛑 Shell 工具与文件搜索红线（严格执行）

1. **禁止递归扫描文档**：
   - 严禁无过滤执行 `rg --files`、`find` 或任何针对 `*.md` 的全局扫描。
   - 已知文件名、目录或扩展名时，允许使用带精确路径或 `-g` 限定的 `rg --files`。
   - 寻找代码时使用具体函数名、文件名、目录或扩展名限定范围，禁止无目标的通配符全仓搜索。

## 授权与工作区

- 审查、分析和计划保持只读；只有用户明确要求实现、修改或执行时才编辑文件。
- 修改前先检查 `git status --short`，不得覆盖或提交无关的已有改动。
- 未经用户明确要求，不创建分支、不提交、不推送、不发布。
- 本仓库独立于 `x-to-xhs`；不得重新建立源码、测试、构建或运行时依赖。

## 记忆范围说明

- Codex 全局记忆位于仓库外，其读写遵循上层全局规则。
- `memory-bank/**` 是当前仓库的项目文档，不属于 Codex 全局记忆。
- 本文件只规定项目 Memory Bank 的授权边界；具体维护流程见
  `memory-bank/README.md`。

## 项目文档加载路由

以下文件均为当前仓库的项目文档，不是 Codex 全局规则，也不会被 Codex
自动加载。只有满足触发条件时才按明确路径读取。

| 项目文档 | 强制触发条件 | 读取时机 |
|---|---|---|
| `docs/development/validation.md` | 用户已授权修改项目文件；或明确要求浏览器、视觉、移动端验收 | 第一次编辑或调用相关验证工具之前 |
| `memory-bank/README.md` | 当前任务需要读取、写入或归档 `memory-bank/**`；或用户要求查看下一任务 | 访问其他 Memory Bank 文件之前 |

## 阅读与修改纪律

- 只读取完成当前任务必需的规则、源码和上下文；使用精确检索，不做无目标全仓扫描。
- 修改前优先选择最小方案；不为单一调用方创建抽象或新增依赖。
- 编辑后清理由本次变更产生的无用代码、导入和测试。
- 访问 `memory-bank/**` 前必须先读取 `memory-bank/README.md`。
  
## 下一任务发现规则

当用户说“查看下一任务”“看看接下来的任务”或同义表达时：

1. 先读取本文件和 `memory-bank/README.md`，再只读取
   `memory-bank/progress.md` 的“当前待办”部分。
2. 选择并复述第一个未完成项目，说明预计范围与验收结果。
3. “查看”保持只读；只有用户明确要求“执行”“实现”“修改”或“编码”时才编辑文件。
4. 不读取已完成列表、归档或其他 Memory Bank 文件，除非首个待办本身明确需要。

## 产品与安全边界

- 扩展只在用户主动点击后读取当前 X/Twitter 页面 DOM；不调用 X API、oEmbed、公共代理或自建上传服务。
- 内容只用于当前扩展会话中的预览和用户主动复制；预览数据只可使用 `chrome.storage.session`，读取后必须删除。
- 不收集、不记录或上传用户正文、Cookie、令牌、账号信息和剪贴板内容。
- 不添加远程脚本、远程代码执行、跟踪分析或不必要的网络权限。
- `manifest.json` 的权限、主机范围和版本号是发布契约。新增权限或扩大 `host_permissions` 必须说明原因、补测试并获得用户明确授权。

## 模块契约

| 模块 | 职责 |
|---|---|
| `manifest.json` | Manifest V3、最小权限和 X/Twitter 匹配范围 |
| `content.js` | 用户触发后的 DOM 提取、折叠内容展开与 Capture 结构 |
| `markdown.js` | Capture blocks 到 Markdown 的纯转换 |
| `popup.*` | 当前标签页校验、采集触发、复制与会话写入 |
| `preview.*` | 一次性会话数据的展示、复制与删除 |
| `test/` | Markdown、选择器兼容性与 Manifest 权限回归 |

- `markdown.js` 必须保持无浏览器页面依赖，以便 Node 测试直接执行。
- `content.js` 的选择器变更必须保留正文、标题、列表、引用和代码块的既有 Markdown 语义；图片默认不进入复制结果。
- Popup、Content Script 与 Preview 的消息/存储键变更属于契约变更，必须同步测试和 `memory-bank/techContext.md`。

## 验证与文档

- 文档或规则变更：运行 `git diff --check`。
- Markdown、采集、Manifest 或交互逻辑变更：运行 `npm test` 和 `git diff --check`。
- 除非用户明确要求浏览器或视觉验收，不调用浏览器自动化；自动化测试不替代真实 X 页面兼容性验收。
- 架构、权限、数据边界或模块契约变化时，同步更新 `memory-bank/techContext.md` 和 `memory-bank/activeContext.md`；完成可验证任务后更新 `memory-bank/progress.md`。
